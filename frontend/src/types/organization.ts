export type Organization = {
    createdDate: string
    id: string
    name: string
    updatedDate: string
}

export type Department = {
    createdDate: string
    id: string
    name: string
    updatedDate: string
}

export type DepartmentUser = {
    avatar: string
    email: string
    gravatarHash: string
    id: string
    name: string
    role: string
}
